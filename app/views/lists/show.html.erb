<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @list.name %>
</p>

<%- if @list.products.any? %>
  <%= render @list.products %>
<%- else %>
  <p>No products found yet.</p>
<%- end %>

<hr />

<%= render "products/form", :list => @list, :product => Product.new  %>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>