<%= div_for product do %>
  <%= product.done %> <%= product.name %> <%= product.amount %> Added <%= time_ago_in_words product.created_at %> ago [<%= link_to 'Apagar', [@list, product], :confirm => 'Are you sure?', :method => :delete, :remote => true %>]
<% end %>